{% extends 'show.html.twig' %}

{% block title %}VirementInternational{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-8 offset-md-2">
                <div class="card">
                    <div class="card-header bg-primary text-white">
                        <h1 class="card-title">VirementInternational</h1>
                    </div>
                    <div class="card-body">
                        <table class="table table-bordered">
                            <tbody>
                                <tr>
                                    <th style="width: 200px;">Ref</th>
                                    <td>{{ virement_international.ref }}</td>
                                </tr>
                                <tr>
                                    <th>TauxEchange</th>
                                    <td>{{ virement_international.tauxEchange }}</td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                    <div class="card-footer">
                        <a href="{{ path('app_virement_international_index') }}" class="btn btn-secondary">Back to list</a>
                        <a href="{{ path('app_virement_international_edit', {'ref': virement_international.ref}) }}" class="btn btn-primary">Edit</a>
                    </div>
                </div>
                {{ include('virement_international/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
