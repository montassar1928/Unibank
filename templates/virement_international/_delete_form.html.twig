<form method="post" action="{{ path('app_virement_international_delete', {'ref': virement_international.ref}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ virement_international.ref) }}">
    <button class="btn">Delete</button>
</form>
